ARCH="ppc64"
CHOST="powerpc64-unknown-linux-gnu"
# This is an experimental profile so we set the ~ keyword
# Since there is no corresponding gentoo-prefix profile, disable the ppc64
# keyword set by the base Gentoo profile
ACCEPT_KEYWORDS="-ppc64 ~ppc64-linux"

SYMLINK_LIB=""
LIBDIR_ppc64="lib"
DEFAULT_ABI="default"
